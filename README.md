# carry_sprint
CarrySprint is a local resident sprint server that provides a stable, reproducible API for personal and team workflows. It acts as a structural backbone—not a task manager—enabling clients such as Obsidian, VSCode, or CLI tools to synchronize sprint states, metrics, and narrative context through a single binary, SQLite-backed, schema-stable core.
